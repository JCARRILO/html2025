#!/usr/bin/env bash
set -e
if command -v pwsh >/dev/null 2>&1; then
  pwsh -NoProfile -File tools/check-encoding.ps1
else
  powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/check-encoding.ps1
fi
